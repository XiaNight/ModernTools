<base:PageBase x:Class="ArmouryProtocol.ProtocolStressTest"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ArmouryProtocol"
             xmlns:base="clr-namespace:Base.Pages;assembly=Base"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:Background="White">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock
            Text="Send a single HID command repeatedly to the currently selected device."
            Opacity="0.9"
            Margin="0,0,0,10" />

        <Border Grid.Row="1" Padding="12" CornerRadius="6" BorderThickness="1" BorderBrush="#33FFFFFF">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left column: HID interface targeting -->
                <StackPanel Grid.Column="0" Margin="0,0,12,0">
                    <TextBlock Text="Target HID interface" FontWeight="SemiBold" Margin="0,0,0,6" />

                    <UniformGrid Columns="2" Margin="0,0,0,8">
                        <TextBlock VerticalAlignment="Center" Text="Usage Page (hex)" />
                        <TextBox x:Name="UsagePageTextBox" Text="FF00" Width="140" />

                        <TextBlock VerticalAlignment="Center" Text="Usage Id (hex)" />
                        <TextBox x:Name="UsageIdTextBox" Text="0001" Width="140" />
                    </UniformGrid>

                    <TextBlock Text="If blank, the page will try to use the first HID interface on the selected device." Opacity="0.7" TextWrapping="Wrap"/>

                    <TextBlock Text="Command (hex bytes, space-separated)" FontWeight="SemiBold" Margin="0,12,0,6" />
                    <TextBox x:Name="CommandTextBox"
                             Text="12 00 00 00"
                             AcceptsReturn="False"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             MinHeight="36"
                             FontFamily="Consolas" />
                    <TextBlock Text="Example: 12 00 00 00" Opacity="0.5" Margin="0,4,0,0" FontSize="11"/>
                </StackPanel>

                <!-- Right column: timing and control -->
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Stress test parameters" FontWeight="SemiBold" Margin="0,0,0,6" />

                    <UniformGrid Columns="2" Margin="0,0,0,8">
                        <TextBlock VerticalAlignment="Center" Text="Interval (ms)" />
                        <TextBox x:Name="IntervalMsTextBox" Text="10" Width="140" />

                        <TextBlock VerticalAlignment="Center" Text="Duration (seconds, 0=∞)" />
                        <TextBox x:Name="DurationSecondsTextBox" Text="10" Width="140" />

                        <TextBlock VerticalAlignment="Center" Text="Max sends (0=∞)" />
                        <TextBox x:Name="MaxSendCountTextBox" Text="0" Width="140" />

                        <TextBlock VerticalAlignment="Center" Text="Wait for response" />
                        <CheckBox x:Name="WaitForResponseCheckBox" VerticalAlignment="Center" IsChecked="False" Checked="WaitForResponseCheckBox_Changed" Unchecked="WaitForResponseCheckBox_Changed" />

                        <TextBlock VerticalAlignment="Center" Text="Response timeout (ms)" />
                        <TextBox x:Name="ResponseTimeoutMsTextBox" Text="100" Width="140" IsEnabled="False" />

                        <TextBlock VerticalAlignment="Center" Text="Retry count" />
                        <TextBox x:Name="RetryCountTextBox" Text="0" Width="140" IsEnabled="False" />
                    </UniformGrid>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,4,0,0">
                        <Button x:Name="StartButton" Content="Start" Width="90" Margin="0,0,8,0" />
                        <Button x:Name="StopButton" Content="Stop" Width="90" IsEnabled="False" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <TextBlock Text="Elapsed:" Opacity="0.8"/>
                        <TextBlock x:Name="ElapsedTextBlock" Text="00:00:00.000" Margin="6,0,0,0" FontFamily="Consolas" Opacity="0.9"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <TextBlock Text="Sends:" Opacity="0.8"/>
                        <TextBlock x:Name="SendCountTextBlock" Text="0" Margin="6,0,0,0" FontFamily="Consolas" Opacity="0.9"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <TextBlock Text="Fails:" Opacity="0.8"/>
                        <TextBlock x:Name="FailCountTextBlock" Text="0" Margin="6,0,0,0" FontFamily="Consolas" Foreground="#FFE07070" Opacity="0.9"/>
                    </StackPanel>

                    <TextBlock x:Name="StatusTextBlock" Text="Idle" Margin="0,6,0,0" Opacity="0.8" TextWrapping="Wrap"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</base:PageBase>

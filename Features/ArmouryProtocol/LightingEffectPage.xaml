<base:PageBase x:Class="ArmouryProtocol.LightingEffectPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ArmouryProtocol"
             xmlns:base="clr-namespace:Base.Pages;assembly=Base"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:Background="White">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock
            Text="Send Armoury RGB Lighting test frames to the currently selected device."
            Opacity="0.9"
            Margin="0,0,0,10" />

        <Border Grid.Row="1" Padding="12" CornerRadius="6" BorderThickness="1" BorderBrush="#33FFFFFF">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,12,0">
                    <TextBlock Text="Target HID interface" FontWeight="SemiBold" Margin="0,0,0,6" />

                    <UniformGrid Columns="2" Margin="0,0,0,8">
                        <TextBlock VerticalAlignment="Center" Text="Usage Page (hex)" />
                        <TextBox x:Name="UsagePageTextBox" Text="FF00" Width="140" />

                        <TextBlock VerticalAlignment="Center" Text="Usage Id (hex)" />
                        <TextBox x:Name="UsageIdTextBox" Text="0001" Width="140" />
                    </UniformGrid>
                    
                    <TextBlock Text="If blank, the page will try to use the first HID interface on the selected device." Opacity="0.7" TextWrapping="Wrap"/>

                    <UniformGrid Columns="3" Margin="0,0,0,8">
                        <TextBlock VerticalAlignment="Center" Text="Save Profile" />
                        <Button x:Name="SaveButton" Content="Save" Width="90"/>
                        <TextBox x:Name="SaveTextBox" Text="01" Margin="6 0"/>
                        
                        <TextBlock VerticalAlignment="Center" Text="Erase Profile"/>
                        <Button x:Name="EraseButton" Content="Erase" Width="90"/>

                    </UniformGrid>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Effect parameters" FontWeight="SemiBold" Margin="0,0,0,6" />

                    <UniformGrid Columns="2" Margin="0,0,0,8">
                        <TextBlock VerticalAlignment="Center" Text="Frame count" />
                        <TextBox x:Name="FrameCountTextBox" Text="60" Width="140" />

                        <TextBlock VerticalAlignment="Center" Text="Command interval (ms, supports decimals)" />
                        <TextBox x:Name="IntervalMsTextBox" Text="10" Width="140" />

                        <TextBlock VerticalAlignment="Center" Text="RGB change speed (step)" />
                        <TextBox x:Name="StepTextBox" Text="64" Width="140" />
                    </UniformGrid>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,4,0,0">
                        <Button x:Name="SendOnceButton" Content="Send one frame" Width="140" Margin="0,0,8,0" />
                        <Button x:Name="StartButton" Content="Start" Width="90" Margin="0,0,8,0" />
                        <Button x:Name="StopButton" Content="Stop" Width="90" IsEnabled="False" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <TextBlock Text="Elapsed:" Opacity="0.8"/>
                        <TextBlock x:Name="ElapsedTextBlock" Text="00:00:00.000" Margin="6,0,0,0" FontFamily="Consolas" Opacity="0.9"/>
                    </StackPanel>

                    <TextBlock x:Name="StatusTextBlock" Text="Idle" Margin="0,6,0,0" Opacity="0.8" TextWrapping="Wrap"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Margin="0,12,0,0" Padding="12" CornerRadius="6" BorderThickness="1" BorderBrush="#33FFFFFF">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Last sent" FontWeight="SemiBold" Margin="0,0,0,6" />
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBox x:Name="LastSentTextBox" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" />
                </ScrollViewer>
            </DockPanel>
        </Border>
    </Grid>
</base:PageBase>

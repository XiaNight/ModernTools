# ArmouryProtocol

ASUS Armoury **lighting protocol** — advanced RGB lighting effects, presets, and profiles, plus a protocol stress test. References the `KeyboardHallSensor` project. Protocol-specific logic stays here; shared transport (HID/USB) comes from `Base/Infrastructure/Peripheral/`.

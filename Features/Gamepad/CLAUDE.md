# Gamepad

Gamepad / controller visualization via **XInput** — joysticks, triggers, buttons, directional input, with live charts. Pages derive from `PageBase` and use the `Update()` lifecycle hook for per-frame polling. Keep visualization work in `Update()` lightweight.

<UserControl x:Class="MouseATE.Pages.AteSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="820" d:DesignWidth="960"
             FontFamily="Consolas"
             Padding="16"
             Loaded="UserControl_Loaded">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Orientation="Vertical">

            <!-- ── Global Test Settings ───────────────────────────── -->
            <GroupBox Header="Global Test Settings" Margin="0 0 0 12">
                <Grid Margin="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" MinWidth="180"/>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="Auto" MinWidth="180"/>
                        <ColumnDefinition Width="110"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Test Distance (mm):" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0 4"/>
                    <TextBox x:Name="GTestDistanceBox" Grid.Row="0" Grid.Column="1" Margin="4 4 0 4"/>

                    <TextBlock Text="Test Speed (0-800):" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0 4"/>
                    <TextBox x:Name="GTestSpeedBox" Grid.Row="1" Grid.Column="1" Margin="4 4 0 4"/>

                    <TextBlock Text="Test Cycles:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0 4"/>
                    <TextBox x:Name="GTestCyclesBox" Grid.Row="2" Grid.Column="1" Margin="4 4 0 4"/>

                    <TextBlock Text="Default Z Offset (mm):" Grid.Row="0" Grid.Column="3" VerticalAlignment="Center" Margin="16 4 0 4"/>
                    <TextBox x:Name="GZOffsetBox" Grid.Row="0" Grid.Column="4" Margin="4 4 0 4"/>

                    <TextBlock Text="Dev Threshold (%):" Grid.Row="1" Grid.Column="3" VerticalAlignment="Center" Margin="16 4 0 4"/>
                    <TextBox x:Name="GThresholdBox" Grid.Row="1" Grid.Column="4" Margin="4 4 0 4"/>

                    <TextBlock Text="LOD Height (mm):" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0 4"/>
                    <TextBox x:Name="GLodHeightBox" Grid.Row="3" Grid.Column="1" Margin="4 4 0 4"/>

                    <TextBlock Text="LOD Interval (mm):" Grid.Row="3" Grid.Column="3" VerticalAlignment="Center" Margin="16 4 0 4"/>
                    <TextBox x:Name="GLodIntervalBox" Grid.Row="3" Grid.Column="4" Margin="4 4 0 4"/>

                    <StackPanel Orientation="Horizontal" Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="5" Margin="0 8 0 0">
                        <Button Content="Save Global Settings" Width="200" Click="SaveGlobal_Click"/>
                        <TextBlock x:Name="GlobalSaveStatus" Margin="12 0 0 0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </GroupBox>

            <!-- ── Device Profiles ───────────────────────────────── -->
            <GroupBox Header="Device Profiles" Margin="0 0 0 12">
                <Grid Margin="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Profile list -->
                    <StackPanel Grid.Column="0">
                        <ListBox x:Name="ProfileList" Height="260"
                                 SelectionChanged="ProfileList_SelectionChanged"/>
                        <StackPanel Orientation="Horizontal" Margin="0 4 0 0">
                            <Button Content="+ Add"    Width="70" Click="AddProfile_Click"    Margin="0 0 4 0"/>
                            <Button Content="✕ Remove" Width="80" Click="RemoveProfile_Click" Margin="0 0 4 0"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Profile edit form -->
                    <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
                        <Grid x:Name="ProfileEditPanel" IsEnabled="False">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" MinWidth="180"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Profile Name:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0 4"/>
                            <TextBox x:Name="PNameBox" Grid.Row="0" Grid.Column="1" Margin="4 4 0 4"/>

                            <TextBlock Text="Device PID (hex):" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0 4"/>
                            <TextBox x:Name="PDevPidBox" Grid.Row="1" Grid.Column="1" Margin="4 4 0 4"/>

                            <TextBlock Text="Dongle PID (hex):" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0 4"/>
                            <TextBox x:Name="PDonglePidBox" Grid.Row="2" Grid.Column="1" Margin="4 4 0 4"/>

                            <TextBlock Text="USB Interface (hex):" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0 4"/>
                            <TextBox x:Name="PUsbIfBox" Grid.Row="3" Grid.Column="1" Margin="4 4 0 4"/>

                            <TextBlock Text="Calibration DPI:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Margin="0 4"/>
                            <TextBox x:Name="PCalDpiBox" Grid.Row="4" Grid.Column="1" Margin="4 4 0 4"/>

                            <TextBlock Text="DPI Step:" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center" Margin="0 4"/>
                            <TextBox x:Name="PDpiStepBox" Grid.Row="5" Grid.Column="1" Margin="4 4 0 4"/>

                            <TextBlock Text="Max LOD Level:" Grid.Row="6" Grid.Column="0" VerticalAlignment="Center" Margin="0 4"/>
                            <TextBox x:Name="PMaxLodBox" Grid.Row="6" Grid.Column="1" Margin="4 4 0 4"/>

                            <TextBlock Text="Media Review DPIs:" Grid.Row="7" Grid.Column="0" VerticalAlignment="Center" Margin="0 4" ToolTip="Comma-separated list, e.g. 400,800,1600,3200"/>
                            <TextBox x:Name="PMrDpisBox" Grid.Row="7" Grid.Column="1" Margin="4 4 0 4" ToolTip="Comma-separated: 400,800,1600,3200"/>

                            <TextBlock Text="Full Test Min DPI:" Grid.Row="8" Grid.Column="0" VerticalAlignment="Center" Margin="0 4"/>
                            <TextBox x:Name="PFtMinBox" Grid.Row="8" Grid.Column="1" Margin="4 4 0 4"/>

                            <TextBlock Text="Full Test Max DPI:" Grid.Row="9" Grid.Column="0" VerticalAlignment="Center" Margin="0 4"/>
                            <TextBox x:Name="PFtMaxBox" Grid.Row="9" Grid.Column="1" Margin="4 4 0 4"/>

                            <TextBlock Text="Full Test Step:" Grid.Row="10" Grid.Column="0" VerticalAlignment="Center" Margin="0 4"/>
                            <TextBox x:Name="PFtStepBox" Grid.Row="10" Grid.Column="1" Margin="4 4 0 4"/>

                            <StackPanel Orientation="Horizontal" Grid.Row="11" Grid.Column="0" Grid.ColumnSpan="2" Margin="0 8 0 0">
                                <Button Content="Save Profile" Width="130" Click="SaveProfile_Click"/>
                                <TextBlock x:Name="ProfileSaveStatus" Margin="12 0 0 0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </ScrollViewer>
                </Grid>
            </GroupBox>

        </StackPanel>
    </ScrollViewer>
</UserControl>

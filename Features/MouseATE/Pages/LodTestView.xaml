<UserControl x:Class="MouseATE.Pages.LodTestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900"
             FontFamily="Consolas"
             Padding="16">
    <UserControl.Resources>
        <Style x:Key="PassCellStyle" TargetType="TextBlock">
            <Setter Property="Text" Value="FAIL"/>
            <Setter Property="Foreground" Value="Red"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Pass}" Value="True">
                    <Setter Property="Text" Value="PASS"/>
                    <Setter Property="Foreground" Value="LimeGreen"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Orientation="Vertical">

            <!-- ── Test Configuration ─────────────────────────────── -->
            <GroupBox Header="Test Configuration" Margin="0 0 0 12">
                <Grid Margin="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" MinWidth="160"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="24"/>
                        <ColumnDefinition Width="Auto" MinWidth="160"/>
                        <ColumnDefinition Width="120"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Target DPI:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0 4"/>
                    <TextBox x:Name="TargetDpiBox" Grid.Row="0" Grid.Column="1" Text="1600" Margin="4 4 0 4"/>

                    <TextBlock Text="Start Height (mm):" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0 4"/>
                    <TextBox x:Name="StartHeightBox" Grid.Row="1" Grid.Column="1" Text="3.0" Margin="4 4 0 4"/>

                    <TextBlock Text="Step Interval (mm):" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0 4"/>
                    <TextBox x:Name="IntervalBox" Grid.Row="2" Grid.Column="1" Text="0.1" Margin="4 4 0 4"/>

                    <TextBlock Text="Path Radius:" Grid.Row="0" Grid.Column="3" VerticalAlignment="Center" Margin="16 4 0 4"/>
                    <ComboBox x:Name="RadiusCombo" Grid.Row="0" Grid.Column="4" Margin="4 4 0 4">
                        <ComboBoxItem Content="50 mm  (Procedure 1)" IsSelected="True"/>
                        <ComboBoxItem Content="100 mm (Procedure 2)"/>
                        <ComboBoxItem Content="150 mm (Procedure 3)"/>
                    </ComboBox>

                    <TextBlock Text="Pass Threshold (%):" Grid.Row="1" Grid.Column="3" VerticalAlignment="Center" Margin="16 4 0 4"/>
                    <TextBox x:Name="ThresholdBox" Grid.Row="1" Grid.Column="4" Text="80" Margin="4 4 0 4"/>

                    <TextBlock Text="Output Folder:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0 4"/>
                    <TextBox x:Name="OutputFolderBox" Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="3"
                             Text="" Margin="4 4 0 4"/>
                    <Button x:Name="BrowseBtn" Content="Browse..." Grid.Row="3" Grid.Column="4"
                            Width="80" HorizontalAlignment="Right" Click="BrowseBtn_Click" Margin="4 4 0 4"/>
                </Grid>
            </GroupBox>

            <!-- ── Run ────────────────────────────────────────────── -->
            <GroupBox Header="Test Run" Margin="0 0 0 12">
                <StackPanel Margin="8">
                    <StackPanel Orientation="Horizontal" Margin="0 0 0 8">
                        <Button x:Name="RunBtn" Content="▶ Run LOD Test" Width="160"
                                Click="RunBtn_Click" Margin="0 0 12 0"/>
                        <Button x:Name="CancelBtn" Content="■ Cancel" Width="100"
                                Click="CancelBtn_Click" IsEnabled="False" Margin="0 0 16 0"/>
                        <TextBlock x:Name="RunStatus" Text="Ready" VerticalAlignment="Center"/>
                    </StackPanel>
                    <ProgressBar x:Name="Progress" Height="8" Minimum="0" Maximum="100" Value="0"/>
                </StackPanel>
            </GroupBox>

            <!-- ── Results ────────────────────────────────────────── -->
            <GroupBox Header="Results" Margin="0 0 0 12">
                <StackPanel Margin="8">
                    <DataGrid x:Name="ResultsGrid" AutoGenerateColumns="False"
                              IsReadOnly="True" Height="240"
                              HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                              FontSize="12" Margin="0 0 0 8">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Height (mm)" Binding="{Binding HeightMm, StringFormat={}{0:F2}}" Width="100"/>
                            <DataGridTextColumn Header="Measured Radius" Binding="{Binding MeasuredRadiusMm, StringFormat={}{0:F1} mm}" Width="130"/>
                            <DataGridTemplateColumn Header="Status" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Style="{StaticResource PassCellStyle}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" MinWidth="160"/>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="24"/>
                            <ColumnDefinition Width="Auto" MinWidth="160"/>
                            <ColumnDefinition Width="160"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Determined LOD:" Grid.Column="0" VerticalAlignment="Center" Margin="0 4"/>
                        <TextBlock x:Name="LodText" Grid.Column="1" VerticalAlignment="Center" FontWeight="Bold" Margin="4 4 0 4"/>
                        <TextBlock Text="Overall Result:" Grid.Column="3" VerticalAlignment="Center" Margin="16 4 0 4"/>
                        <TextBlock x:Name="OverallResultText" Grid.Column="4" VerticalAlignment="Center" FontWeight="Bold" Margin="4 4 0 4"/>
                    </Grid>

                    <Button x:Name="ExportBtn" Content="Export CSV" Width="120"
                            HorizontalAlignment="Left" Click="ExportBtn_Click"
                            Margin="0 8 0 0" IsEnabled="False"/>
                </StackPanel>
            </GroupBox>

        </StackPanel>
    </ScrollViewer>
</UserControl>

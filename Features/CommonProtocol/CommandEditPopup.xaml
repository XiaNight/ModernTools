<UserControl x:Class="Base.UI.Pages.CommandEditPopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             mc:Ignorable="d"
             d:DesignWidth="460" d:DesignHeight="200">

    <!-- 
        The visual content is placed inside a Popup that is opened/closed
        programmatically. The UserControl itself renders nothing inline;
        it only owns the Popup instance.
    -->

    <Popup x:Name="PopupRoot"
           StaysOpen="False"
           AllowsTransparency="True"
           Placement="Bottom"
           PopupAnimation="Fade">

        <Border CornerRadius="6"
                Padding="4"
                BorderThickness="1"
                Background="{DynamicResource SystemControlBackgroundAltHighBrush}"
                BorderBrush="{DynamicResource SystemControlForegroundBaseMediumLowBrush}">

            <StackPanel Margin="8">
                <!-- Header -->
                <TextBlock Text="Commands (one per line, hex bytes, max 64 bytes each):"
                           FontSize="12"
                           Margin="0,0,0,4"
                           Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}"/>

                <!-- Multi-line hex editor -->
                <TextBox x:Name="CommandEditBox"
                         AcceptsReturn="True"
                         TextWrapping="NoWrap"
                         FontFamily="Consolas"
                         FontSize="12"
                         MinWidth="420"
                         MinHeight="120"
                         MaxHeight="300"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"/>

                <!-- Interval setting -->
                <DockPanel Margin="0,8,0,0" LastChildFill="False">
                    <TextBlock DockPanel.Dock="Left"
                               Text="Interval between commands:"
                               FontSize="12"
                               VerticalAlignment="Center"
                               Margin="0,0,8,0"
                               Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}"/>
                    <TextBox x:Name="IntervalBox"
                             DockPanel.Dock="Left"
                             FontSize="12"
                             Width="60"
                             TextAlignment="Center"
                             VerticalContentAlignment="Center"
                             MaxLength="5"/>
                    <TextBlock DockPanel.Dock="Left"
                               Text="ms"
                               FontSize="12"
                               VerticalAlignment="Center"
                               Margin="4,0,0,0"
                               Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>

                    <!-- Save button -->
                    <Button x:Name="SaveButton"
                            DockPanel.Dock="Right"
                            Content="Save"
                            Margin="0,6,0,0"
                            VerticalAlignment="Center"
                            MinWidth="80"/>
                </DockPanel>
            </StackPanel>
        </Border>
    </Popup>
</UserControl>

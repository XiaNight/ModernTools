// The declarative protocol model that used to live here (Listener, Structure, Data<T>
// and the concrete data types) was promoted into Base so it can be shared across
// features. It now lives in Base/Infrastructure/Protocol (namespace Base.Protocol).
// This file is intentionally left empty; reference the types via `using Base.Protocol;`.

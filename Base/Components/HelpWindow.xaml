<Window x:Class="Base.Components.HelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Base.Components"
        mc:Ignorable="d"
        Title="Help"
        Height="400" Width="600"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow" 
        ShowInTaskbar="False"
        MinWidth="300"
        MinHeight="200"
        Topmost="True">
    <DockPanel Margin="8">
        <TextBlock x:Name="DescriptionText" DockPanel.Dock="Top" Text="Description Text" Margin="0,0,0,8" TextWrapping="Wrap"/>
        <TextBox x:Name="FilterBox" DockPanel.Dock="Top" Margin="0,0,0,8"/>
        <ListView x:Name="HelpList"
              IsTextSearchEnabled="True"
              MouseDoubleClick="List_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Control" DisplayMemberBinding="{Binding ControlPath}" Width="260"/>
                    <GridViewColumn Header="Tooltip" DisplayMemberBinding="{Binding Tooltip}" Width="200"/>
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</Window>

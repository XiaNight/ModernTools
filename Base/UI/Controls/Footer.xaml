<UserControl x:Class="Base.Components.Footer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:core="clr-namespace:Base.Core"
             xmlns:local="clr-namespace:Base.Components"
             xmlns:controls="clr-namespace:Base.UI.Controls"
             mc:Ignorable="d"
             d:DesignHeight="24" d:DesignWidth="800" d:Background="White">
    <Grid Height="26" Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}">
        <DockPanel LastChildFill="False" x:Name="ContentPanel" Margin="0">
            <TextBlock x:Name="DeviceName"
		               Text="Disconnected"
		               FontSize="14"
                       Margin="10,0"
		               VerticalAlignment="Center"
		               DockPanel.Dock="Left"/>

            <TextBlock x:Name="DeviceVersion"
		               Text="FW: ----"
		               FontSize="14"
                       Margin="10,0"
		               VerticalAlignment="Center"
		               DockPanel.Dock="Left"
                       Visibility="Collapsed"/>
            <controls:BatteryIndicator x:Name="BatteryIndicator"
                                       Margin="10,0"
                                       DockPanel.Dock="Left"
                                       Visibility="Collapsed"/>
        </DockPanel>
    </Grid>
</UserControl>

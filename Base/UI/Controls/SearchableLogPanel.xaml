<UserControl x:Class="Base.Components.SearchableLogPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Base.Components"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Search bar -->
        <Border Grid.Row="0" Padding="8" Background="{DynamicResource SystemControlAcrylicWindowBrush}" BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="SearchBox"
                         MinWidth="220"
                         VerticalContentAlignment="Center"
                         Text="{Binding SearchText, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged}"
                         KeyDown="SearchBox_KeyDown"/>

                <TextBlock Grid.Column="1" MinWidth="20" Margin="10,0,0,0" VerticalAlignment="Center"
                           Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}"
                           Text="{Binding MatchLabel, RelativeSource={RelativeSource AncestorType=UserControl}}"/>

                <Button Grid.Column="2"
                        Margin="8,0,0,0" Padding="6"
                        Click="Prev_Click"
                        ToolTip="Previous (Shift+Enter)">
                    <ui:FontIcon FontSize="18" Glyph="&#xE76B;" FontFamily="Segoe Fluent Icons" /> <!-- ChevronLeft -->
                </Button>

                <Button Grid.Column="3"
                        Margin="6,0,0,0" Padding="6"
                        Click="Next_Click"
                        ToolTip="Next (Enter)">
                    <ui:FontIcon FontSize="18" Glyph="&#xE76C;" FontFamily="Segoe Fluent Icons"/> <!-- ChevronRight -->
                </Button>

                <Button Grid.Column="4"
                        Margin="16,0,0,0" Padding="6"
                        Click="Clear_Click"
                        ToolTip="Clear log">
                    <ui:FontIcon FontSize="18" Glyph="&#xE74D;" FontFamily="Segoe Fluent Icons"/> <!-- Delete -->
                </Button>
            </Grid>
        </Border>

        <!-- Log area -->
        <Grid Grid.Row="1">
            <RichTextBox x:Name="LogBox"
                         IsReadOnly="True"
                         IsDocumentEnabled="False"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled"
                         Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                         BorderThickness="0"
                         FontFamily="Consolas"
                         FontSize="14"
                         Padding="8"/>
        </Grid>
    </Grid>
</UserControl>
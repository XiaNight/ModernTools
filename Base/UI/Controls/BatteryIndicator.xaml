<UserControl x:Class="Base.UI.Controls.BatteryIndicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Base.UI.Controls"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="100">
    <ui:SimpleStackPanel Orientation="Horizontal" Spacing="4">
        <ui:FontIcon x:Name="BatteryIcon"
                    FontSize="20"
                    FontFamily="Segoe Fluent Icons"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}"
                    Glyph="&#xEBB0;" />
        <TextBlock x:Name="BatteryText"
                    Text="100%"
                    FontSize="14"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}" />
    </ui:SimpleStackPanel>
</UserControl>

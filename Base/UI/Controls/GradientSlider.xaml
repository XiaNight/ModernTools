<UserControl x:Class="Base.UI.Controls.GradientSlider"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 mc:Ignorable="d"
			 d:DesignWidth="200" d:DesignHeight="18"
			 MinHeight="16">
	<Canvas x:Name="Root"
			Background="Transparent"
			SizeChanged="Root_SizeChanged"
			MouseLeftButtonDown="Root_Down"
			MouseMove="Root_Move"
			MouseLeftButtonUp="Root_Up">
		<!-- The track is filled with the gradient built from the colour marks.
		     Its inset and the knob's travel are computed in code so the knob
		     centre lands on the corner-radius centre at each end. -->
		<Border x:Name="Track" CornerRadius="4" BorderThickness="0" BorderBrush="#40000000"/>
		<Ellipse x:Name="Thumb" Width="14" Height="14"
				 Stroke="{DynamicResource SystemControlForegroundBaseMediumBrush}" StrokeThickness="2" Fill="#20000000"
				 IsHitTestVisible="False"/>
	</Canvas>
</UserControl>

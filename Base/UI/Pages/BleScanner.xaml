<!-- BleScanner.xaml -->
<UserControl x:Class="Base.Pages.BleScanner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListView
            x:Name="DeviceList"
            IsTextSearchEnabled="True"
            ItemsSource="{Binding Devices}"
            GridViewColumnHeader.Click="DeviceList_OnColumnClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="260"/>
                    <GridViewColumn Header="Address" DisplayMemberBinding="{Binding AddressFormatted}" Width="200"/>
                    <GridViewColumn Header="RSSI (dBm)" DisplayMemberBinding="{Binding Rssi}" Width="100"/>
                    <GridViewColumn Header="Last Seen" DisplayMemberBinding="{Binding LastSeenFormatted}" Width="180"/>
                    
                    <!-- Action button column -->
                    <GridViewColumn Header="Action" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Connect"
                                        Padding="4,2"
                                        Command="{Binding DataContext.ConnectCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>